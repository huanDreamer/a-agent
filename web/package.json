{
  "name": "huan-agent-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "huan-agent Web Admin UI (Vue 3 + Vite, embedded into the Go binary)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:ui": "node scripts/check-component-bindings.mjs && node scripts/check-budget.mjs && node scripts/check-stats.mjs && node scripts/check-artifacts.mjs && vite build --config ssr-probe/vite.config.js && node ssr-probe/run.mjs",
    "check:budget": "node scripts/check-budget.mjs",
    "check:stats": "node scripts/check-stats.mjs",
    "check:artifacts": "node scripts/check-artifacts.mjs"
  },
  "dependencies": {
    "dompurify": "^3.4.15",
    "marked": "^18.0.12",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0"
  }
}
